pluginManagement {
    repositories {
        maven { url = 'https://maven.aliyun.com/nexus/content/groups/public' }
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven { url = 'https://maven.aliyun.com/nexus/content/groups/public' }
        google()
        mavenCentral()
    }
}

rootProject.name = "RunBeatPro"
include ':app'
